# Audio assets (required for P0 sound features)

Place the following files here (filenames must match):

| File | Use | Notes |
| --- | --- | --- |
| `hero-loop.mp3` | Hero ambient / intro loop | ~30–60s, loopable, preferably stereo |
| `the-return-teaser.mp3` | Music section + “THE RETURN.” card | 15–30s teaser |

Optional fallback formats (same basename):
- `hero-loop.ogg`
- `the-return-teaser.ogg`

Until these files are supplied by Roy, the site hides audio controls gracefully and does not show broken players. The card click falls back to the first platform link (Spotify).

Do not commit placeholder/fake binary audio.
